# Technical architecture

## 1. Decisions and boundaries

Use Electron + React + TypeScript + Vite, npm workspaces, Vitest for domain/integration checks, Playwright's Electron support for the small end-to-end suite, and @electron/packager for distributable folders. Vite builds the renderer and worker; build main/preload as separate Node-targeted bundles with a minimal esbuild configuration. Do not add Next.js, React server components, a web backend, SQLite or an ORM initially. Keep the simulation state in memory and serialize whole stable checkpoints; revisit storage only against the measured ten-season save-size/performance gate.

This is a design choice for a table-heavy management game and a TypeScript workflow. Electron carries a larger runtime and requires active security/native-module maintenance. Godot becomes attractive if interactive 3D football becomes the product, but that is outside this design. Tauri adds a Rust boundary and differing system webviews; there is no demonstrated need to trade Electron's shared Chromium behavior for it here. The choice is reasoned, not a benchmark claim.

Select exact compatible stable package releases at project initialization and commit a lockfile. Do not bake an unverified React release from the prior conversation into the project. Record Electron/Chromium/Node, React, Vite, TypeScript, builders, test runners and native Steam binding versions in the decision log. Use official documentation for their selected releases. Supported version selection is a v0.1 task, not an assumption that package versions never change. Sources: [Electron security](https://www.electronjs.org/docs/latest/tutorial/security), [React](https://react.dev/learn), [Vite](https://vite.dev/guide/), [Electron Packager](https://packages.electronjs.org/packager/).

## 2. Repository layout

```text
README.md                                # public game overview and actual status
AGENTS.md, PROJECT_BRIEF.md, docs/         # seven agent/specification documents
.gitignore                               # generated/private exclusions
apps/game/src/main/                       # lifecycle, file persistence, Steam adapter
apps/game/src/preload/                    # narrow typed bridge
apps/game/src/renderer/                   # React screens and transient state
apps/game/src/worker/                     # serial simulation host, no Node access
apps/roster-tool/                         # separate developer-only local tool, v0.6
packages/simulation/src/                  # pure game functions and deterministic RNG
packages/contracts/src/                   # schema validation, IDs, DTOs, versions
packages/presentation/src/                # event projection; pixel-art event highlights and text
packages/roster-pipeline/src/             # normalization, adapters, data quality checks and provenance
assets/original/                         # original art/audio with provenance
data/fictional/                           # small committed development fixtures
tests/                                   # shared risk scenarios; obey AGENTS ceilings
work/                                    # ignored build/research/probe artifacts
```

Tests may instead be colocated when clearer, but never duplicate shared risk scenarios. Licensed raw responses, tokens and private contracts are outside version control and shipping paths. Public cleared packs are separate release artifacts, not fixtures embedded in unit tests. Build allowlists must exclude roster-tool, provider code, raw data and credentials from the game.

## 3. Runtime topology and authority

```text
React screen -> typed preload request -> main validation/router -> simulation worker
                                                            <- result/view model
worker stable checkpoint -> main save writer -> local filesystem
worker events -> presentation projector -> pixel art / text
main -> optional Steam adapter
publisher roster tool -> provider adapters -> staging/review -> cleared pack
cleared pack -> main validates -> worker creates NEW career
```

Main is privileged I/O only. One Web Worker owns one active career and processes commands serially. Main creates/routes a transferred MessagePort during trusted renderer startup; renderer cannot replace the authoritative host with arbitrary external content. A simpler initial implementation may let the renderer relay typed worker messages to main, but main revalidates every privileged request and trusts no arbitrary path or payload. Worker can restart from the most recent confirmed stable save; unexpected worker failure stops play with recovery choices, not silently generates a new career.

Simulation exports `createCareer(snapshot, seed, rules)`, `applyCommand(state, command)` and `advanceUntil(state, stopCondition)`. Functions return new state plus domain events, or a structured error with no mutation. No global mutable state. Within a tick, collect outcomes then commit once; use structural sharing where useful instead of cloning the entire world per player operation. Commit full state to disk only at stable boundaries.

Commands: SelectLineup, SetTactics, AdvanceDay, StartMatch, AdvanceMatch, Substitute, SubmitOffer, CounterOffer, ConfirmDeal, SetTraining, ScoutPlayer, UpgradeFacility, AcceptJob. Payload always includes commandId, careerId and expected stateRevision. Return new revision, result/error, events and necessary view projections. Reject stale revision with `STALE_STATE`, refresh UI, never retry spending automatically. Keep an applied-command ID ring of last 256 commands in the save; revision checks prevent older requests from being reapplied outside that ring.

The authoritative match advances only to the next requested tick/chunk. Never simulate future goals ahead of an allowed tactical interruption. Automatic playback requests one minute at a time. No public instant-finish or single-step controls; half-time continuation is a renderer pacing preference. Mandatory decisions still stop playback. Each worker chunk yields within 50 ms. Visualization can lag; it cannot skip mandatory decision stops or mutate the match.

## 4. Reproducibility and versions

Use one documented uint32 PRNG implementation with test vectors (e.g. xoshiro128**, carefully preserving unsigned arithmetic). Seed domain-separated streams for fixtures, matches, transfer AI, injuries, youth and development with a stable hash, not host-language hash functions. Save all active stream state. Match streams are keyed by fixture ID so scheduling UI presentation does not alter other matches. The presentation RNG is entirely independent.

No `Math.random()`, `Date.now()`, locale comparisons, unordered iteration or wall-clock-based decisions in simulation. Sort by normalized ASCII stable IDs, never localized names. Store probabilities in basis points and use deterministic rounding rules; clamp at boundaries. The same commands must reproduce the same domain checksum across the supported Windows/Linux builds. Do not promise replay compatibility across different engine versions.

Versions are independent:

| Version | Meaning / change rule |
| --- | --- |
| appVersion | Shipped application semantic version |
| saveSchemaVersion | Integer disk structure; migration required for change |
| engineVersion | Domain behavior implementation; changes when outcomes can change |
| rulesetVersion | Balance and competition parameters; copied into a career |
| rosterSchemaVersion | Integer normalized pack structure |
| snapshotId/contentHash | Identity and integrity of immutable starting data |

Updating roster packs never modifies a career. Updating the engine may change future outcomes even with unchanged rules. Pre-1.0 saves are supported from the immediately previous milestone with an explicit migration; older unsupported development saves fail helpfully and remain on disk. From 1.0, support forward migrations for all 1.x saves. Retain source file and back up before migration. Migration creates a new commit, records old/new engine and schema versions, and tells the player when simulation behavior changes. No downgrading writes; reject future schemas read-only.

Current Career schema is 24, app 0.9.0, engine 0.7.4 and rules world-2; Dream envelope is 4 with its separate mode state. The decision log records subsequent migrations; the following summarizes earlier supported development formats. Schema 9 gains empty scouting state and retains its existing date/condition/cash; no recovery is reversed. Schema 8 gains deterministic fictional contract metadata without changing wages or ledger history. Schema 7 gains generated wage commitments and a balanced opening ledger at its current date; no historical income or costs are invented. Schema 6 gains explicit half phases and zero already-completed added time, preserving its elapsed ticks and past events. Schema 5 adds empty dismissal/injury/decision records, no bans and no retrospective incident generation. Schema 4 upgrades to condition 100, morale 70 and balanced training; live matches gain per-player condition at that boundary, without rewriting past events. Schema-3 saves retain tactics/history and gain a default career bench, three empty setup slots and the managed club ID; schema-1/2 migrations chain through that upgrade. Schema-1 saves derive benches and empty substitution history; schema-2 saves retain benches/history. Both explicitly migrate to balanced tactics after original checksum and legacy-schema validation. New saves require all tactics/bench/history fields; missing current fields are invalid. Load reports an engine change to the player. Future outcomes may differ under the tactical rules. Save history links record original/new versions; source checkpoints remain untouched.

## 5. Persistence and recovery

Use gzip-compressed canonical UTF-8 JSON, with SHA-256 checksum of uncompressed canonical payload. Canonicalization sorts object keys and ID-keyed collections, preserves explicitly ordered event arrays and excludes volatile timestamps from domain hash. File envelope contains schema, app/engine/rules versions, careerId, saveCommitId, parentCommitId, stateRevision, savedAtUTC, snapshot identity, checksum and payload. Payload includes complete mutable world, rules, remaining fixtures, RNG states and bounded history. It must not require fetching the original roster pack to load a career.

Directory under Electron userData: `saves/<careerId>/`, machine settings separately in `settings/`. Save filenames are immutable unique commit IDs, never club names or unsanitized imported text. Maintain three latest autosaves, manual slots and one pre-migration backup. Enforce one active game instance to prevent concurrent local writers.

Write a new same-directory `.tmp`, flush the file handle, validate by reading/checksum, then rename to a new `.save` filename that does not already exist. Only complete `.save` files are candidates. An optional index is reconstructible and never the only source of truth. Do not overwrite the only good save, depend on platform-specific replacement rename, or prune before the new commit verifies. Interrupted write leaves the previous complete commit intact. On startup ignore temporaries; if the newest commit is corrupt, offer the previous valid one and preserve the corrupt file for diagnosis. Disk-full/permission errors show unsaved progress and Retry/Save Copy options; never display "saved" before completion.

Autosave after a match, weekly stable boundary and season rollover; manual Save is available whenever no atomic command is in progress. Saving mid-match records exact tick, pending decisions, lineups and RNG; resuming must not reapply finance or injury events. Quit waits for an in-flight save or lets the player explicitly quit without saving after failure.

Steam Auto-Cloud syncs complete save files only; exclude `.tmp`, settings, caches, raw provider data and logs. Use Steam user-specific roots and Windows/Linux root overrides when configuring cloud. Immutable filenames preserve branches rather than a single contested mutable file. At load, two valid commits with the same ancestor but neither descending from the other are a conflict: show dates, season, club and progress, offer either or Keep Both. Do not automatically merge careers or choose by device clock. Prune only recognized ancestors after successful local writes; never delete an unresolved branch. Steam may present its own conflict UI before game launch; game-level detection supplements it. Test actual cross-OS sync rather than assuming it from configuration. [Valve cloud documentation](https://partner.steamgames.com/doc/features/cloud?l=english).

## 6. Presentation contract

Pure highlight selection curates committed events without domain RNG use. The renderer stages an anticipation still, cuts to the actual outcome, then clears ordinary action scenes. Blue/white represents the managed side; red/white the opponent. The broadcast temporarily delays score, stats and commentary during anticipation, never authoritative career state. Save/Load always uses authoritative state. No Canvas animation, Three.js or WebGL runtime remains.

MatchScreen supplies statistics and team management; MatchVisual composes four equal panels: statistics/team management on the left, commentary/highlights on the right. Score, clock and Play/Pause occupy a separate content-sized row above the board. Images fit their allocated space without moving other panels. Narrow windows stack the panels. Missing artwork retains commentary; still images use no spatial animation. Paused previews do not write to the worker or save store. Pause/hidden state freezes ordinary chance timing; loading does not replay old events.

Local audio assets and source credits are centralized, and playbackRate stays 1 independently of the fixed 2x match pace. One recorded ambience loop runs during Play. Further sound design is deferred by the owner. Historical decisions below describe superseded presentation experiments; GAME holds the current art bible.

## 7. Security and distribution

Renderer: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`. Use a strict production CSP, local packaged resources and no remote script execution. Disable unexpected navigation/new windows and permissions. Validate requesting sender/frame for every IPC handler. Expose named methods, not raw `ipcRenderer`, shell execution or arbitrary filesystem access. Limit parsed save/pack size and nesting. Render external names as text, not HTML. No external URLs opened without an allowed HTTPS scheme and explicit user action.

Keep `steamworks.js` behind main-process `SteamService` and a no-op development adapter. The repository's Electron example relaxes renderer isolation; **do not copy that configuration**. Its native binding and overlay need packaged compatibility proof on both OSs. If secure main-process integration fails, retain the secure boundary, record the missing feature and evaluate the adapter; do not silently enable Node in the renderer. [Binding source](https://github.com/ceifa/steamworks.js/) and [Electron's security guidance](https://www.electronjs.org/docs/latest/tutorial/security).

Build native Windows and Linux folders; Steam delivers those folders through separate depots with executable permissions retained on Linux. Developer preview may use ZIP/tar or AppImage, but Steam runtime validation is a separate gate. Build Linux on Linux against the selected Steam Linux Runtime compatibility target and Windows on Windows. Verify native module ABI, redistributable paths, writable saves outside the install folder, case-sensitive asset paths and absence of developer-machine absolute paths. Do not promise Proton as a substitute for native Linux. [Valve Linux guidance](https://partner.steamgames.com/doc/store/application/platforms/linux).

Use Steam for application/roster-pack updates, not an independent Electron auto-updater. No mandatory network entitlement check in gameplay. Steam-specific achievement delivery may retry next connected session from local unlocked IDs. Achievement proposals are First Win, Promotion, Domestic Champion, Cup Winner, Continental Champion and Ten Seasons. No anti-cheat or leaderboards in an offline moddable game.

## 8. Performance targets and diagnostics

Reference baseline to obtain by v0.1: Windows 11 and Ubuntu 26.04 on a four-core x64 machine with 8 GB RAM, SSD and integrated graphics; record actual CPU/GPU/driver/OS versions in ROADMAP. This is a development target, not a published minimum spec yet.

Warm menu actions p95 <100 ms; first launch to menu <5 seconds excluding Steam startup; 10-division ordinary day <500 ms and matchday <2 seconds with presentation disabled; new season <5 seconds; stable save/load <2 seconds each; game memory <800 MB; ten-season compressed career <25 MB. Idle menus must not run a free animation loop. Pixel-art highlights use still-image cuts at 1280*720 and above; keep input responsive while decoding images, without a continuous animation loop.

Measure five warm repetitions for ordinary performance checks; report machine, medians/p95 and dataset. Long-career runs follow AGENTS fixed workload, not continuous benchmarking. Diagnostics opt-in export includes versions, error codes, timings and checksums; save content only with player consent. No telemetry server at launch. Current diagnostics keep only 100 whitelisted operation records in memory and write only on explicit local export; no persistent raw-error log exists. If persistent logging is added, rotate at 5 MB *3 and redact credentials/provider URLs with tokens. Retain coarse season records while pruning old detailed events; verify no retained scene growth after ten match-screen mount/unmount cycles.

## 9. Steam Deck and Dream Club extensions

### Steam Deck and controller implementation

v0.2 implements the shared keyboard/standard-gamepad adapter using the [W3C Gamepad standard mapping](https://www.w3.org/TR/gamepad/). Synthetic Electron input verifies modal navigation and held-confirm suppression; it does not certify physical controllers. Native Tab remains available alongside directional focus and Q/E or shoulder section navigation. Reports read finalized state and need no new persistence schema.

Target Ubuntu 26.04 desktop separately from SteamOS on actual Steam Deck hardware. Use the current SteamOS stable release installed at validation and record its version. Check the shipped native Linux build through Steam and its selected Steam Linux Runtime; do not assume an Ubuntu package behaves identically on Deck. If Valve evaluates a different launch path, record and test that path explicitly.

Controller actions: move focus, confirm, back, next/previous section, open contextual actions, continue and pause match. Playback stays at the owner-selected 2x. Centralize them in one input adapter. Use Steam Input mappings and appropriate controller glyphs; all features must be reachable without an external keyboard/mouse. Deck trackpads are supplementary, not the sole solution for inaccessible controls. Use Steam text input/onscreen keyboard for names and search; restore focus when it closes. The offline fallback provides a controller-driven English keyboard and native Unicode text field, plus a modal option chooser. It changes only renderer form drafts through the existing input/change boundary; simulation commands still validate submitted values. Never require a desktop launcher to choose a mode.

Provide a 1280x800 handheld layout: essential text >=16 physical pixels as an internal design target, single-column player panels, readable table summaries with detail views, generous focus targets and all primary actions onscreen. Prefer 1280x800; 1280x720 remains supported. Verify text legibility on the actual screen, not only screenshots. Highlights use still-image cuts and no continuous Canvas render loop; keep input responsive during image decoding and retain text fallback. Pause and checkpoint on suspend when possible, and always support interruption recovery because suspension may arrive without time to save. On resume restore audio availability, controller focus and pending pack choices, leaving match playback paused.

v0.8 manual pass: title-to-match, transfer, search/name entry, save/load, Dream Club pack choice, suspend/resume and offline play on real Deck. v0.9 submit the compatible build for Valve review and record outcome/actions. Deck Verified is the desired external result, not a badge the project can self-certify. Unless the owner changes this requirement, waiting for the external badge alone does not block the Windows/Linux release; fix demonstrated compatibility defects and describe tested support accurately. Official references: [compatibility guidance](https://partner.steamgames.com/doc/steamhardware/compat?l=english) and [controller recommendations](https://partner.steamgames.com/doc/steamhardware/recommendations?language=english).

### Dream Club state isolation

Add `gameMode: career | dreamClub` to the versioned save envelope. Add mode-specific commands EarnFixtureReward (internal only), RevealPack, ChoosePlayer and SetCollectionSquad. Use a separate reward RNG stream and persist it, earned fixture IDs, progress remainder, pack sequence, queued pack IDs, pending candidate IDs and unlocked-player IDs. Resolve reward draws outside the match RNG. Validate ChoosePlayer against the saved pending candidates; atomically commit the unlock and consumed pack ID. Retry returns the existing result, never a second player. Do not redraw from a new roster snapshot when loading an old save.

At fixture completion atomically commit result, reward progress and new pack entitlement. Revealing the next queued pack also commits its candidates before animation begins. This lets unrevealed queued rewards avoid players unlocked from earlier packs without allowing revealed results to reroll. Put reward rules in a pure Dream Club module; no provider calls, online account or payment interface. Presenter effects consume saved results only. Mode dispatch runs only its own progression processors; sharing match code must not accidentally activate Career wages/development in Dream Club.

## 10. Decision log - edit here

| ID | Decision | Status / revisit |
| --- | --- | --- |
| A01 | Electron/React/TS/Vite; pure worker simulation | Accepted design; exact versions pending v0.1 |
| A02 | Windows + native Linux | Owner-required; both platform gates mandatory |
| A03 | Three.js first-look, shared event stream and fallback | Owner-required experiment; preference pending demo |
| A04 | Compressed complete file checkpoints | Accepted baseline; revisit only if measured limits fail |
| A05 | Approved city-based fictional identity profile by default | Real-name profile optional; rights/provider arrangements assumed handled |
| A06 | Fixed seven-document set and test ceilings | Accepted working contract |
| A07 | Fictional second tiers and simplified rules | Proposed baseline; owner can replace before competition scope expansion |
| A08 | Ubuntu 26.04 LTS desktop; actual Steam Deck validation | Updated owner preference; Steam Linux Runtime remains separate |
| A09 | Separate Dream Club with earned choice packs | Owner-selected; bounded v0.7 prototype, no purchases or shared Career economy |
| A10 | Basic ASCII authored documentation | Owner-requested; preserve Unicode in roster/localization data |
| A11 | Football Club Universe (FCU); immutable identity profile per save | Owner-selected title and city-based naming direction; no renaming stable IDs when branding changes |
| A12 | Main-only GitHub backup after completed changes | Owner-directed; no Actions/PRs/worktrees, plain descriptive commit subjects |
| A13 | Public README separate from tracked PROJECT_BRIEF | Owner-directed; all guidance tracked, generated/private paths ignored |
| A14 | 2026-09-11: exact registry-verified stable dependencies: Electron 44.3.0, React/DOM 19.3.0, Vite 8.3.0, TypeScript 7.0.2, Three.js 0.186.0, esbuild 0.28.2, electron-builder 26.15.3, Vitest 5.0.0, Playwright 1.63.0, Zod 4.6.2, ESLint 10.10.0, typescript-eslint 8.70.0 | v0.1; Owner requested latest stable tools. TypeScript 7.0.2 runs tsc through @typescript/native; the documented npm alias supplies @typescript/typescript6 6.0.2 only for the ESLint API. Host Node 26.8.2/npm 11.19.1. No native Steam binding. Official runtime/API docs checked; exact pins/lockfile authoritative. |
| A15 | 2026-09-11: Mulberry32 uint32 RNG with FNV-1a domain-separated fixture match seeds; minute ticks and explicit half-time stop | v0.1 engine/rules pinned. Conditions fixed at 100 and morale at 70 (effective multiplier 1.04); cards, injury, tactics and substitutions deferred to their gates. Synthetic roster is fixed across career seeds. |
| A16 | 2026-09-11: renderer relays typed worker checkpoints to a sender-validated main save service; sandboxed CJS preload, ESM main | v0.1 permitted relay topology. Main parses all disk payloads. Typecheck explicitly invokes the 7.0.2 binary to avoid npm compatibility-package bin collisions. Electron runtime: Chromium 152.0.7977.78 / Node 24.20.0. |
| A17 | 2026-09-11: immutable checkpoints retained in v0.1, including all autosaves, with explicit recovery selection | Conservative first playable policy preserves older and branched files. Three-autosave pruning deferred until ancestry-aware cleanup; disk use grows within this bounded single season. No pre-v0.1 schema exists to migrate. |
| A18 | 2026-09-11: lazy plain Three.js with separate stable visual offset, original primitives, bounded animation and context-loss cleanup | v0.1 experiment only. No simulation dependency on rendering or external art. 30 fps default, 60 optional, shadows off. Reduced-motion starts in text. Owner evaluation pending. |
| A19 | 2026-09-12: replace electron-builder with @electron/packager 20.3.0 for native portable folders; npm 12.0.2, explicit esbuild 0.28.2 script approval and strict script policy | Removes the deprecated installer/asar/proxy dependency chains rather than overriding incompatible transitive APIs. Bundle-only staging and unique release/build-* outputs; start:packaged reads release/latest.json. Save root explicitly remains appData/fcu regardless of packaging productName. Three.js core split into a separate lazy chunk, no warning suppression. |
| A20 | 2026-09-12: owner prioritizes original retro game presentation before new mechanics; replace sidebar with a bounded game shell and action boards | First visual batch only; Three.js retained. Orthographic horizontal camera fits the stadium footprint; goal cut-in is ephemeral committed-event presentation and does not replay on loading. No engine/rules/save migration. Richer stadium and event storytelling await batch review. |
| A21 | 2026-09-12: original Web Audio square/triangle phrases and short SFX, optional music and independent effects toggle | No audio library or external recordings. Music starts on user input, voices are released after playback, all sounds stop while hidden or on unmount. Settings are session-only in this first audition; richer soundscape and persistent volume controls remain later visual work. |
| A22 | 2026-09-12: owner approved the retro shell and requested continuously maintained reference documents; stadium batch follows | Pure samplePlay projects committed events into positions; Three.js owns rendering only. Instanced supporters, flag pivots and local canvas boards share resources with explicit disposal. Animation runs only during playback or bounded three-second replay, pauses when hidden, and never sends simulation commands. No engine/save version change. |
| A23 | 2026-09-12: owner requests fullscreen, larger stadium scale and fluent match pacing; removes skip controls | Fullscreen default with native F11 toggle and fluid shell; full-pitch camera may crop outer decks. Pure choreography accepts previous visual positions; articulated block figures and instanced crowd arms stay presentation-only. Six-second highlights and 1.2-second quiet ticks at 1x, capped 3x; optional half-time continuation. Engine/rules/save versions unchanged. Supersedes A22 replay duration (now six seconds). |
| A24 | 2026-09-12: owner selects pixel-art highlights and requests removal of unused tools | Supersedes A03/A18/A20/A22/A23 presentation experiments. Remove Three.js, its types, stadium, figure projector, choreography, GPU controls and build split. Original Canvas 2D goal/save/miss audition plus text; labeled previews never affect careers. Engine/rules/save versions unchanged. |
| A25 | 2026-09-12: owner requests ISS Deluxe-informed anatomy and less repetitive miss cutaways | Original jointed sprite poses with six-frame gait, shaded slim bodies and planted-leg kick. Every third miss is eligible; goals/saves take priority within a minute. All attempts stay recorded; no probability/rules/save change. Off Target replaces the unsupported narrow-miss claim. |
| A26 | 2026-09-12: central commentary, fixed 2x, running clock and bottom-to-top highlights | Remove mode/speed selectors and idle art. Shared minuteDuration sets 600 ms quiet ticks or 3000 ms selected chances. Clock seconds are transient interpolation clamped before the next minute, frozen on pause and reset to tick:00 on load. Real highlights overlay for 2.4 seconds, then reveal commentary. No engine/rules/save change; blocks/post outcomes deferred. |
| A27 | 2026-09-12: persistent commentary beside highlights and prominent metrics | Enlarge score/clock; left event feed and right statistics with temporary highlights above. Adapt metric rows to a two-column comparison during clips and scale clips for short windows. No engine, clock, save or playback changes. |
| A28 | 2026-09-12: fixed highlight frame and close attacker/keeper scene | Reserve frame and label height at all times, retaining two-column metrics. Remove defender and unused opponent-color projection. Show approach, shot and reactions on the same pitch; no penalty or simulation changes. |
| A29 | 2026-09-12: large left highlights, right commentary/statistics and shared idle scenery | Split the right column vertically, keeping statistics compact and stationary. Use the same Canvas painter for idle scenery and active scenes; remove the separate CSS pitch. Idle draws once, with no animation or domain writes. |
| A30 | 2026-09-12: v0.2 begins with paused substitutions and schema-2 checkpoints | Freeze nine-player benches at kickoff; enforce five changes, three distinct non-half-time ticks and no re-entry. Store ordered substitutions separately from chance events, preserving existing highlight logic and RNG. Engine/app 0.2.0, unchanged exhibition-1 probabilities. Schema-1 decode validates the original checksum then explicitly adds benches/empty history; subsequent saves are schema 2 and old files remain intact. |
| A31 | 2026-09-12: registered test budgets enforced by both runner configurations | Before execution, discover Vitest and Playwright registrations without executing bodies. Combine counts and file paths, including parameterized cases. v0.2 limits 45 cases, nine files and two Electron journeys; empty/failed discovery fails closed. No source-text counting or new registry document. |
| A32 | 2026-09-12: formation and tactical control slice | SetTactics changes only future ticks and next-fixture defaults, without RNG draws or replacing participants. Natural-slot assignment and 0.80 mismatch role strength; chance factors follow GAME, pressing modifies M. Original lineup order remains the weighted-draw order. Schema 3 supports explicit schema-1/2 migrations; preserve source files and show changed-engine notice. Fatigue costs, AI minute-60 policy and custom saved presets deferred. |
| A33 | 2026-09-12: match planning and opponent decisions | SelectBench validates nine unique club reserves and one GK outside the committed eleven. Lineup edits retain eligible reserves; kickoff freezes them. Three nullable tactic slots persist through StoreTacticPreset without applying tactics or consuming RNG. Match managedClubId keeps the minute-60 score-based AI policy away from human controls and allows both AI sides in background fixtures. Schema 4 migrates schema 1/2/3 explicitly, preserving original checkpoints. |
| A34 | 2026-09-12: result report and shared navigation | Reports project committed results/standings without domain writes. Keyboard and standard Gamepad API map to common actions; spatial focus skips unavailable controls, dialogs contain Back and restore focus, held confirm cannot repeat. Poll only a connected standard controller while visible. Native text editing is retained; Steam Input glyph/text-entry and hardware polish remain v0.8. No save or engine change. |
| A35 | 2026-09-12: condition, morale and recovery | Condition uses integer thousandths, retaining fractional per-tick costs. Recompute effective strength each tick; unused reserves do not tire. Finalized fixtures update morale and condition before seven daily recovery steps. Training changes through a command. Schema 5 migrates older plans without changing prior events. Owner now authorizes successive roadmap milestones; external checks remain honest open evidence. |
| A36 | 2026-09-12: availability and league discipline | Keep active exclusions and pending decisions in Match, future bans/recovery dates in players. Serve existing league bans before adding new sanctions on fixture completion. Injuries stop managed play; replacements or explicit short-handed continuation clear the decision. Up to nine bench players, eight bounded fictional academy call-ups and depletion forfeits avoid healing/duplicating players. Kickoff clones lineup arrays and uses one dated selection for both lineup and bench. Schema 6 chains earlier migrations. Added time and balance remain open. |
| A37 | 2026-09-12: real added time and calibration | Elapsed ticks reach at most 100; explicit first/interval/second/finished phases govern playback, completion and free half-time substitutions. Freeze each half's added time at regulation end from its goals, injuries and substitutions, capped at five. Forfeits retain their actual elapsed tick. Schema 7 preserves prior matches with zero historical added time. Exhibition-6 raises conditional conversion from .30 to .36, lowers direct-red conditional probability from .015 to .01 and weights already-booked foul candidates at .35. This addresses low goals and excessive dismissals without altering committed scores. Background second halves use the roster including academy additions. |

| A38 | 2026-09-12: financial foundation | Store wage commitments and balanced, uniquely identified ledger postings in career state. Derive cash and reserves rather than persist duplicate balances. Home gates and daily operating settlements run in the existing round transaction for all clubs. Values are generated game estimates, initialized independently of match RNG. Migration starts an opening ledger at the saved date; schema 8 keeps source saves immutable. Future contracts will supply age, wage and ownership terms. Club finances uses shared game tokens and modal navigation. Registered v0.4 ceilings are 65 cases, 12 files and three journeys. |

| A39 | 2026-09-12: reviewed contract renewals | Contract metadata is keyed by player ID; wages remain authoritative only in economy.wages. Review computes terms without mutation. RenewContract checks ownership, contract revision, term, role, wage budget and post-signing cash reserve; one uniquely keyed signing-bonus posting precedes the contract update. Schema 9 preserves existing money and match history. Ability helpers move to a pure ratings module so economic calculations do not depend on the command engine. Owner explicitly requests continued autonomous work across milestone boundaries. |

| A40 | 2026-09-12: between-fixture calendar and scouting | Explicit day/event advancement processes recovery and finance without simulating fixtures. Scouting has one active seven-day job and stable uncertain ranges independent of match RNG. Save schema 10 preserves previous already-advanced dates. Decode uses an ordered format table pairing each version with its rules and migration, replacing repeated header conditions without weakening checksum/header checks. Clubhouse uses four game action tiles, and reports stay accessible after calendar progress. |

Append a dated row only for consequential changes: reason, alternatives rejected, affected gate and migration impact. Routine implementation details belong in code.

| A41 | 2026-09-12: staged transfers and historical membership | Schema 11 adds offers, explicit nullable free-agent ownership/expiry and match-time club membership. Past lineup/event validation uses that frozen membership, so later transfers do not invalidate history. Registration posts one balanced fee and bonus, changes ownership/wages/contract revision atomically and invalidates competing offers. Calendar processes club replies, expiry and confirmed window queues. Migrations append twelve deterministic free agents without repricing existing wages or altering past events. Renderer review remains non-authoritative; all changes use worker commands. Loans and weekly recruitment follow separately. |

| A42 | 2026-09-12: bounded recruitment and actionable news | Daily calendar work resolves AI contract terms; Mondays in an open window may generate two deficit targets per AI club. IDs derive from seed/date/club/slot and pending offers count as cover. Candidates exclude the managed squad. Recruitment uses scouting estimates and the shared deal checks/ledger, leaving match RNG untouched. Schema 12 migrates transfer state unchanged except engine/rules identity. News is a bounded projection of offers/reports with player links, not a second authoritative event store. |

| A43 | 2026-09-12: loan ownership and return | Schema 13 separates the contract owner from the player's registered club only through an active loan. Offers carry a nullable wage share; completed loans keep parent, borrower and June-30 return. Wages remain one authoritative amount, split for Monday postings; future cash and wage reserves retain the parent's full obligation. Registered squad capacity includes reserved return places. Calendar returns loans before daily settlement and refreshes the managed borrower's lineup/bench using the shared pure selection module. Validation cross-checks offers, loans, contracts, ownership and historical match membership. |

| A44 | 2026-09-12: repeatable season transaction | Schema 14 adds dated fixtures, season year and immutable result/table archives. New-year fixture IDs include the year, avoiding repeated match streams and gate receipts. Closing pays uniquely keyed league prizes, releases expiry, carries bans, clears yellows, covers shortages and builds the next calendar atomically. Academy identity sequences never reuse IDs of previously released players. Selection, academy, scheduling/ranking and season closing are cohesive pure modules; the engine retains their public exports. The 2026 exhibition keeps its original dates and IDs through migration. Later country/cup expansion uses this foundation. |

| A45 | 2026-09-12: country leagues and bounded annual finance history | Schema 15 stores world/division membership and competition-tagged fixtures. All ten league schedules are authoritative, with background results settled by calendar date. Closing archives divisions, prizes and closing balances before moving two clubs each way; the detailed ledger becomes current-season entries plus balanced opening carry postings. Old schema-14 histories gain exhibition metadata without rewriting source files. Save decompression remains bounded, raised to 32 MiB for the 176-club world. Match ticks reuse a 44-player condition view; operating payments batch duplicate checks; AI searches reuse an unchanged club budget and cached candidate estimates, with full registration checks retained. No presentation-driven simulation or new runtime dependencies. |

| A46 | 2026-09-12: pure cup draw foundation | Cup draws use independently seeded streams keyed by season, competition and round, with sorted inputs before shuffling. Domestic preliminary byes reduce 34/36 entrants to 32; all later rounds are knockout. Continental ties reverse venues on leg two and aggregate without away goals. Reserve distinct Wednesday windows, validate at least three days between every reserved league/cup slot, and keep cup winners separate from penalty scores. Contracts are defined but not persisted until match integration; no save-version change or visible incomplete control. Latest owner scope is finish v0.5, then stop before v0.6. |

| A47 | 2026-09-12: complete cup integration | Schema 16 persists cup draws, fixture class/neutrality/deciding leg, aggregate context, separate cup counters, extra-time phases and eligible penalty kicks. Fixtures, draws, champions and financial archives cross-validate on load. Existing country saves defer cups to the next year through cupStartSeason; exhibition saves remain unchanged. Finals remove home advantage and split gate receipts. Penalties use the match stream but do not alter goals or open-play statistics. Worker commands retain atomic fixture and season settlement. UI presents cup rounds/history, extra time and kick results in the existing match layout. Sorted ASCII IDs determine fixture order without locale-dependent comparison. Shortlists now share the saved population ceiling. Initial gate failures exposed early background results and redundant AI calculations; preserve the scouting assertion and unchanged world-test timeout while fixing scheduling and reusing affordability checks. |

| A48 | 2026-09-12: owner selects detailed still-image highlight sequences | Supersedes the moving-art portions of A24-A29. Four original local PNGs replace the procedural Canvas painter, sprite poses and motion sampler; remove unused files. A pure frame selector maps committed goal/save/shot to preparation then its matching reaction. Renderer timers update only at cuts, freeze on Pause/hidden, retain the last reaction and do not mutate domain state. All four images preload in the reserved panel, with nearest-neighbor filtering and text fallback on load error; reduced motion uses the same nonmoving art. Shared illustrative cast/kit is explicit, actual identity stays in captions. GAME records the owner's art bible; assets/highlights/provenance.json stores built-in generation prompts. No dependency, simulation, rules or save version change. |

| A49 | 2026-09-12: approved art, managed-side palettes and synchronized anticipation | Blue/white is managed, red/white opponent; venue and stored club identities do not change. A renderer broadcast stages only new consecutive minutes, temporarily projecting the prior score/stats/feed during preparation, then revealing authoritative results. Save/Load always operates on authoritative state, never this transient display. Scenes clear after playback and before Play use a shared team lineup. Pause/hidden preserve timing; fallback and Skip reveal current data immediately. Goal keeper reaches the correct side; saves use a high dive. Local synthesized crowd/kick sounds use their own audio noise, never simulation RNG. |
| A50 | 2026-09-12: event-driven scene library and shootout anticipation | 29 still assets cover supported incidents, substitutions, tactical coach, boundaries and chance/shootout outcomes. Priority is dismissal/injury/goal/yellow/save before curated ordinary fouls/misses. Shootout display slices the committed kick list until each reveal; it never edits the career. Pending incident controls reveal after the cut. Only current and outcome images mount, avoiding decoding the whole library. Ambient crowd and scene cues use local Web Audio. Offside, cancelled goals, corners, post hits and in-play penalties require explicit domain events in a separate requested rules slice; no fake events or save/engine changes here. |
| A51 | 2026-09-12: owner match-board sketch and rejected runtime sound synthesis | Statistics/team management form the left column; commentary/highlights form the right. Play/Pause sits beside the clock. Replace runtime effect synthesis with five inspectable original PCM WAV cues; keep optional synthesized menu music. Asset creation has its own fixed seed and never touches simulation. HTML audio playbackRate remains 1 independently of match speed. WAVs and their generation source are tracked; sound quality awaits owner listening review. |
| A52 | 2026-09-12: owner rejects synthesized WAV cues and requests free recordings | Replace all five match effects with edited BigSoundBank/Freesound recordings; remove the rejected generator. audioAssets.ts shares playback URLs and source/author/license/edit metadata with the shipped Sound library. edits.json records source hashes and preparation offsets. Keep PCM, natural pitch and fixed playbackRate 1. One ambience element loops while playing and stops on Pause; never start repeated overlapping long clips. Native audio controls provide direct audition, one clip at a time. Simulation and save state are unchanged. |
| A53 | 2026-09-12: clearer disappointment and sliding tackle cue | Replace stadium protest with CC0 group disappointment, limited to shot/penalty misses. Add CC0 grass-slide/scuff foley to the tackle frame, then existing whistle at the decision cut. Sound library and credits share six assets. Preparation supports named subsets to preserve untouched assets and edit metadata. No engine/save changes. |
| A54 | 2026-09-12: owner rejects disappointment/tackle/shot suitability | Audible context matters more than source labels or signal peaks. Review full-source candidates before replacing cues again; no automatic first-transient excerpt selection for future actions. GAME stores the shortlist. Playback verification cannot certify sound-design quality. Current installed cues remain unapproved. |
| A55 | 2026-09-12: owner approves stadium near miss and selects anime-style action effects | Retain realistic stadium reactions and audition expressive library impacts/whooshes for shots and tackles. Supersedes real-recordings-only action sourcing in A52. Paid libraries are candidates, not authorized purchases. Preserve local assets, provenance and playbackRate 1; no runtime or simulation change in this research slice. Approved near-miss source awaits integration. |
| A56 | 2026-09-12: equal match quadrants and scoreboard overlap correction | Replace fixed scoreboard row heights with intrinsic sizing and an 18px gap. Use equal fractional rows/columns, scroll overflowing panel content and contain still art inside its allocated height. Keep narrow-screen stacking. Audio work deferred; no simulation/save change. |
| A57 | 2026-09-12: v0.6 roster validation foundation | Publisher-only UUID contracts remain separate from legacy gameplay IDs. Strict normalized schema plus private extraction audit gate candidate review; canonical content ignores fetch time while approval covers it. Fixture data reuses centralized city identities. No provider/Node pipeline imports enter simulation or renderer bundles. |
| A58 | 2026-09-12: signed roster archive boundary | Pin fflate 0.8.3 for ZIP compression/streamed expansion; Node crypto owns Ed25519. Exact filename allowlist and ZIP-directory link checks precede bounded in-memory parsing. Signed manifest covers payload/notices hashes. Verified temp files publish through exclusive links; existing snapshots are never overwritten. Official trust roots remain external inputs until release configuration. |
| A59 | 2026-09-12: separate local publisher review UI | Dedicated Vite/Electron entry points and sandboxed preload expose only read/typed actions. Trusted main-frame checks and a serialized action queue protect candidate revision/hash checks. Candidate JSON is private local state; exports are immutable development ZIPs. Publisher output directories are ignored and never enter game packaging. |
| A60 | 2026-09-12: normalized roster import and schema 17 | Pure roster contracts move to contracts/roster.ts. Main validates/install packs; worker maps stable source UUIDs into career-local IDs and creates a new world. Save state pins mappings, hash and observation date. Engine/app 0.6.0 preserves world-2 rules; schema-16 saves migrate with null roster origin and retain original files. Existing careers are never synchronized to new packs. |
| A61 | 2026-09-12: roster registration and imported loans | Schema 18 / engine 0.6.1 adds required registered flags and market/roster loan source. Available-player selection excludes inactive seniors while finances retain their wages. SetRegistration is an atomic command on a cloned state, limited by return reservations, with two keepers and an eligible XI. Imported loans use frozen source identity rather than fake completed offers. Old saves gain registered=true and market source through migration. |

| A62 | 2026-09-12: publisher snapshot comparison | Compare the candidate against the newest locally verified export, ordered by creation time then numeric revision. Filter player changes by selected club and preserve the baseline across restart. Canonicalize set-like lists before diffing to avoid false edits after archive round trips. Live provider verification is deferred by owner; fictional data remains the development path. |

| A63 | 2026-09-12: provider transport isolation | Sportmonks adapter uses explicit season squad endpoints and an editorial position-ID map; unknown roles or contradictory IDs fail. Restricted HTTPS paths, Authorization header, disabled redirects, serialized quota pacing, bounded response size and four retries keep provider I/O publisher-only. Synthetic fixtures prove protocol behavior; live access remains deferred. |

| A64 | 2026-09-12: dated facilities and schema 19 | App/engine 0.7.0 adds a facility record per club. One project at a time posts an exclusive balanced cost and completes after 30 days during calendar/season progression. Existing saves migrate to zero levels without costs or backdated effects. Recovery applies after completion; academy boosts newly created cover. Roster schema-18 content remains accepted by the unchanged normalized import contract. |

| A65 | 2026-09-12: personnel lifecycle and schema 20 | Engine/app 0.7.1 stores potential, minutes, role counters, training logs and dated reports separately from match attributes. Development and youth draws use seed/year/player streams; retirement preserves player/contract identity but disables recruitment. Match settlement records appearances once. Season close develops before expiry and adds four prospects after rollover; academy overflow releases by potential, age and ID. Old saves begin with generated potential and zero historical counters; imported potential is retained. |

| A66 | 2026-09-12: board and manager continuity, schema 21 | Board assessment follows settled league results; debt processing uses balanced, date-keyed loan repayments. Assisted mode prevents dismissal, not costs. Dismissal freezes ordinary commands until a job is accepted or the manager retires. A club change rebuilds selection and scouting while retaining world and manager history. Older saves initialize confidence without historical penalties. |

| A67 | 2026-09-12: derived assistant advice | Advice reads current squad, scouting estimates and the shared deal eligibility/budget checks. It does not consume RNG, persist redundant snapshots or mutate career state. Displayed candidate costs are individual estimates; final transactions revalidate all constraints. |

| A68 | 2026-09-12: isolated Dream reward foundation | A separate strict collection contract records reward RNG, credited fixtures/seasons, queued pack numbers, pending candidates and consumed choices. Pure rewards exclude forfeits and stop at pool exhaustion. The fifth-pack guarantee applies to the first slot when higher-band players remain. Repeating a consumed choice returns the original success without another unlock. Runtime and save integration follow before exposing the mode. |

| A69 | 2026-09-12: playable isolated Dream season | Dream has a dedicated worker, strict schema-1 mode envelope and dream-saves root. It holds a frozen source pool and a shared match-state projection using existing Career-shaped fields for presenter compatibility; unused economy/personnel data never progresses. Only an allowlisted match command set enters the shared engine with Career settlement disabled. Rewards and fixture completion checkpoint together; revealed candidates remain hidden after a failed disk save until retry succeeds. AI fantasy instances use local IDs mapped to source identities. Source ratings remain fixed; next-season roster draws use disclosed tiers with nearest-rating fallback. Career saves and mode commands stay separate. |

| A70 | 2026-09-12: selectable squad depth | Count outfield players plus at most one available goalkeeper when determining lineup size, academy shortage or forfeit eligibility. Counting reserve goalkeepers as field places could make the suggested lineup impossible to confirm after contract turnover. The fixed long-career workload exposed this in its third season. |

| A71 | 2026-09-12: long-career storage and command cost | Career schema 22 / engine 0.7.3 adds compact completed-transfer history. Season close archives permanent deals, retains completed loans for ownership validation and drops ended unsuccessful negotiations. Expired academy contracts release ordinary free agents. Commands copy writable records while sharing immutable archived seasons, transfers and ledger entries; intermediate match ticks copy only match state. Weekly payroll is aggregated once. Recruitment caches only derived ranking inputs and rechecks ownership, squad and finance constraints against current state. Dream envelope 2 upgrades its previous match projection without sharing Career saves. The fixed three-seed workload runs independent workers with immediate progress messages and an unchanged 200-second internal ceiling. |

| A72 | 2026-09-12: training availability and academy notice, schema 23 | Engine 0.7.4 uses a separate seed/date stream and stable eligible player IDs for non-match training injuries, without consuming match RNG. Calendar progression stops on a managed injury. Existing injuryUntil fields remain authoritative; no redundant injury notification state is saved. June academy warnings derive from current contracts and the four-player intake/eight-place rule, labeling overflow as an estimate. Career schema 23 and Dream envelope 3 migrate previous engine identities without backdated injuries or changing pending choices. |

| A73 | 2026-09-13: persistent machine audio preferences | App 0.8.0 stores strict version-1 audio settings in userData/settings/audio.json. Main owns serialization, fsync and atomic rename; interrupted temporary files do not replace the last confirmed settings. Invalid or newer files are preserved and reported, not silently reset. Narrow trusted-frame IPC exposes only validated Music/SFX booleans. Renderer applies saved choices and disables duplicate writes; shutdown awaits pending settings. Career/Dream state and engine versions are unchanged. Owner accepted the Dream prototype pace, allowing v0.8 polish. |

| A74 | 2026-09-13: handheld match space and navigation | Dream matches use one compact mode header and the shared four-panel board; collection/navigation rows remain on management screens. Back pauses and returns to the mode home before exiting it, and closes nested saved/collection views first. Short-display essential match labels use 16px text and HUD targets remain 44px. Reduced-motion suppresses pointer-triggered translation as well as animation/transitions. Windows viewport inspection at 1280x800/720 is not a physical Steam Deck or controller certification. |

| A75 | 2026-09-13: Dream view cleanup and bounded resource evidence | Dream unmount stops its shared ambience, stops playback, terminates its worker and resolves/clears pending request handlers/timeouts. The ten-cycle script reuses a fixed saved club and reports DOM count plus per-process working-set/private memory and a one-second settled sample. [Electron MemoryInfo](https://www.electronjs.org/docs/latest/api/structures/memory-info) reports KB; working sets may overlap shared pages, while private memory excludes them. Preserve both measurements; do not equate ten stable observations with proof of no future leak or hardware certification. |

| A76 | 2026-09-13: Dream choice comparison and focus | Choice cards derive overall-rating differences against the best registered managed player in the same role; this is a comparison, not a promise of improved results. Ratings, reward odds and saved choices are unchanged. Presentation-only focus follows a newly revealed choice to its first button and returns to the primary fixture/season action after selection. Three columns stack on narrow displays. |

| A77 | 2026-09-13: collection registration and handheld squad | Collection filtering and selection drafts remain renderer-only. The existing Squad command validates and saves the confirmed active players, then returns to lineup selection. Collection no longer compresses a second management screen beneath it. Its confirmation header stays outside the scrolling player list; names, roles and ratings use 16px text on handheld layouts. Shared squad actions precede the decorative tactics pitch. |

| A78 | 2026-09-13: offline controller form entry | Standard-controller Confirm opens text/numeric entry or a focusable select chooser. The last open dialog owns navigation, including nested editors. Apply dispatches the same native input/change event as direct editing; Close/Back leaves the original value unchanged and restores focus. The fallback requires no Steam or network dependency. Steam-specific glyphs/SDK text input remain deferred, and synthetic checks do not certify physical hardware. |

| A79 | 2026-09-13: suspend checkpoint boundary | Main forwards only suspend/resume notifications through a removable preload subscription. Both modes stop playback immediately and request a save once an in-flight command has settled, retaining its latest authoritative tick. Career uses an autosave; Dream uses its isolated checkpoint store. Audio silences without changing saved Music/SFX preferences. Resume restores preferences but never automatically starts the match. OS suspend may arrive before disk work completes, so existing immutable recovery checkpoints remain necessary. Synthetic power notifications test the code path, not physical sleep timing. |

| A80 | 2026-09-13: visible save alternatives | Save-list projections expose existing parent commit IDs and season metadata; disk schema and engine remain unchanged. A shared presentation helper marks surviving heads only when their ancestry intersects within the same career. Traversal guards cycles and does not rank choices by wall-clock timestamps. Both modes retain every alternative and checkpoint. This is local recovery visibility, not verified Steam Cloud transport, automatic merging or a pruning policy. |

| A81 | 2026-09-13: private local diagnostics and provenance | Main records only approved operation names, bounded durations and coarse error codes, capped at 100 records. Export includes strict build/runtime metadata and a canonical-payload SHA-256, never command arguments, raw errors, paths or save contents. Native Save dialog selects an explicit local output; nothing is transmitted. Build metadata embeds Git commit/dirty state, dependency-lock hash and renderer-content hash; live Vite development records a null renderer hash. Global utility dialogs pause Dream as well as Career. |

| A82 | 2026-09-13: artifact fingerprints and bundled notices | Vite emits renderer dependency licenses; esbuild input metadata identifies the actual desktop packages whose license texts are retained. Packaging carries those notices and existing audio attribution beside Electron notices. Source-file, lock, renderer and desktop fingerprints must match the recorded build before packaging. Each portable folder gets a validated file-size/SHA-256 manifest; unexpected, changed or omitted files fail verification. Manifests inventory local artifacts, not Steam upload or code-signing certification. |

| A83 | 2026-09-13: save before mode exit | Dream exposes a transient checkpoint callback to the parent shell. Title navigation pauses playback, waits for its pending command, and saves the latest authoritative state before unmounting. Save failure keeps the mode open for retry. Career title navigation also saves dirty state. No modal or renderer callback is stored in domain state, and no existing checkpoint is overwritten. |

| A84 | 2026-09-13: graceful close handshake | Main asks the trusted renderer for an active-mode checkpoint using a single-use request ID. Renderer pauses and waits for a pending command before saving. Main closes only after acknowledgement and store queues drain. A ten-second timeout or failed save opens Retry, Return to game or explicit Quit without saving; old checkpoints remain immutable. Late acknowledgements cannot close a canceled request. Preload strips IPC event objects and removes its subscription on cleanup. |

| A85 | 2026-09-13: independent save writer metadata | Career schema 24 and Dream envelope 4 store the application manifest version separately from engine identity. Both accept previous envelopes without modifying original files or domain state. Schema increments let earlier applications refuse new envelopes safely. Pre-24 Career app metadata historically followed the engine; pre-4 Dream writer metadata is unknown. New app-only updates do not change engine versions or deterministic continuation. |

| A86 | 2026-09-13: management dialog scrolling | Scouting and contracts retain their header/filters while candidate and report panes scroll independently; keyboard focus reveals actions within their pane. Narrow windows use one scrolling content column. Finance retains a sticky Close header above its ledger. Essential role, expiry and ledger text is at least 16 px. No business rules or persisted state change. |

| A87 | 2026-09-13: offline player help | Static localized topics reuse native dialog/details and the shared modal focus lifecycle. Opening help pauses either mode; closing restores the trigger and leaves play paused. Help requires no network, service, new IPC endpoint or persisted state. Content describes implemented controls and explicitly keeps physical-controller verification pending. |

| A88 | 2026-09-13: empty renewal eligibility | The contract dialog can open when a valid academy-only or loan-only squad has no owned senior player. It renders an explanation and Close before calculating terms; no undefined player enters pricing helpers. Renewal rules and state remain unchanged. Verified through a schema-valid isolated save and existing contract invariants. |

| A89 | 2026-09-13: screen-error recovery | A React root error boundary unmounts the failed session, releasing its timers, workers, audio and subscriptions through existing cleanup. A small independent fallback retains standard input, local diagnostic export and explicit return to title. It never serializes potentially broken renderer state or rewrites confirmed checkpoints. Unsaved progress may be lost, stated before recovery. This catches React render/lifecycle errors, not process termination or arbitrary async failures. |

| A90 | 2026-09-13: content-addressed save summaries | Each store caches at most 1000 validated summaries, never full careers or compressed bytes. Lookup hashes current file bytes; a changed hash reruns the existing decoder and identity checks. Returned summaries are copies. Save verification populates the cache, while Load always validates complete state. No disk index, new trust boundary, payload format or retention change. Warm browsing of 105 country checkpoints is 58-67 ms on the reference Windows machine; first restart scan remains about seven seconds. |

| A91 | 2026-09-13: visible checkpoint scans | Career and Dream Load immediately display a busy state and serialize scanning with their existing operation guard. Closing hides the view; late results do not reopen it. All success/error paths release the guard. The underlying read-only scan can finish after Close; no fabricated percentage or empty-history message is shown while waiting. Modal focus remains stable when Career results replace the loading message. |

| A92 | 2026-09-13: checkpoint selection guard | Both modes acquire their operation guard before reading a selected checkpoint. Dream hands the successful result directly to its guarded worker command; there is no event-loop gap between releasing the disk-read guard and acquiring the command guard. Failure paths release controls and retain the prior view/state. Existing confirmed checkpoints remain immutable. |

| A93 | 2026-09-13: current compatible validation/type pins | Zod 4.6.4 replaces 4.6.2 after reviewing [4.6.3](https://github.com/colinhacks/zod/releases/tag/v4.6.3) and [4.6.4](https://github.com/colinhacks/zod/releases/tag/v4.6.4); FCU does not use the removed standalone properties schema. Node definitions 24.13.4 match Electron's bundled Node 24 and deduplicate its dependency; host tooling remains Node 26.8.2. [Definitely Typed versioning](https://github.com/DefinitelyTyped/DefinitelyTyped#how-do-definitely-typed-package-versions-relate-to-versions-of-the-corresponding-library) aligns declarations with their runtime API. Direct pins and lockfile remain exact; clean install, full validation suites and both local builds pass. Zero known audit advisories is evidence, not a vulnerability guarantee. |

| A94 | 2026-09-13: truthful local store drafts | Selected gameplay screenshots are tracked under assets/original/store with source commit, dimensions and SHA-256 provenance. They are unedited content captures from an isolated fictional career using normal UI and actual worker events. Temporary profiles/capture scripts remain ignored. GAME owns draft copy; no extra planning document, gameplay change, Steam upload or publication is implied. |

| A95 | 2026-09-13: diagnostic transport recovery | Report retrieval exposes reading/error/retry states and ignores results after its dialog closes. Both structured errors and rejected IPC promises produce local feedback; export always releases its busy state. Retry performs a new read of the existing bounded report, without introducing data collection or network behavior. |

| A96 | 2026-09-13: native renderer termination recovery | The main-process close coordinator owns renderer-process-gone recovery. It clears pending close acknowledgements/timeouts, prevents requests to a dead renderer and offers explicit title reload or Quit. A pre-existing save-failure prompt is allowed to close before the crash prompt appears; its stale choice cannot request another save. Reload starts a fresh renderer/title, never reconstructing unsaved state. Confirmed checkpoint bytes are preserved. Actual forced renderer termination and checkpoint loading were observed locally; this does not cover loss of the entire OS or main process. |

| A97 | 2026-09-13: native application identity | An original editable SVG FCU mark is retained with hashed PNG/ICO derivatives. The desktop build copies PNG before its artifact hash; Windows packager embeds the six-resolution ICO. BrowserWindow receives the bundled PNG path for native window lists. No runtime image-generation dependency, new brand text constant, installer or shortcut registration is introduced. Native Windows resource extraction confirms the icon; Linux appearance is pending its platform check. |

| A98 | 2026-09-13: cross-platform source byte stability | Repository attributes select LF for text and binary treatment for image/audio/archive assets, overriding machine-specific autocrlf. This preserves original SVG provenance and makes source fingerprint differences reflect content rather than checkout line endings. Existing index text was already LF; no mass renormalization or binary rewrite was performed. Native platform builds still produce their own fingerprints and require independent verification. |

| A99 | 2026-09-13: local SteamPipe preview preparation | npm run steam:preview accepts a portable folder and explicit App/depot IDs, validates the entire file manifest and required executable/archive, then writes an ignored unique preview configuration. Per-file mappings preserve subdirectories and omit later-added files; Preview=1 and no SetLive prevent treating it as a release script. It never invokes Steam, handles credentials or changes the game. Valve example IDs only were used locally; actual account/SteamPipe validation remains unverified. |

Local depot preparation uses `npm run steam:preview -- "portable folder" APP_ID DEPOT_ID`, once actual IDs are available. Run separately for the native Windows and Linux artifacts. Generated VDF files and cache folders remain under ignored work/steam-preview-*; no account data belongs in source. Validate file mappings with the real SteamPipe tools before any separately authorized upload. This is configuration preparation, not a Steam adapter, depot upload, platform install check or release. Format reference: [Valve upload configuration](https://partner.steamgames.com/doc/sdk/uploading), checked on 2026-09-13.

| A100 | 2026-09-13: bounded checkpoint rows | Shared Career/Dream history renders 20 entries per page without filtering, deleting or rewriting checkpoints. Alternate-head detection uses the entire entry list and is memoized across page turns, so a branch on another page remains recognized. Page navigation resets list scroll and moves focus away from a newly disabled boundary button after commit. Disk scanning, validation and immutable retention remain unchanged; cold listing cost is not hidden by pagination. |
| A101 | 2026-09-13: approved title artwork | Reuse the original navy/gold manager illustration through a Vite asset import, retaining one master and its provenance. A contained image preserves the complete composition across aspect ratios; semantic buttons occupy a separate responsive menu band. Remove the unused procedural title stadium and its styles. Simulation, match stills and saves are unaffected. Owner visual acceptance is separate from technical accessibility verification. |
